<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= book.title %> - Review</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-5">
    <a href="/" class="btn btn-secondary mb-4">← Back to Library</a>

    <div class="card shadow">
      <div class="card-body">
        <h2 class="card-title"><%= book.title %></h2>
        <h5 class="card-subtitle mb-2 text-muted">by <%= book.author %></h5>

        <% if (book.rating) { %>
          <div class="mb-3">
            <% for (let i = 0; i < book.rating; i++) { %>
              <i class="fas fa-star text-warning"></i>
            <% } %>
            <% for (let i = book.rating; i < 5; i++) { %>
              <i class="far fa-star text-warning"></i>
            <% } %>
          </div>
        <% } %>

        <% if (book.review) { %>
          <p class="card-text"><%= book.review %></p>
        <% } else { %>
          <p class="card-text text-muted fst-italic">No review available for this book.</p>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
</body>
</html>